        PAGE 60,80
        TITLE ornek____
        
STEK SEGMENT PARA STACK 'STK'
        DW 30 DUP(?)
STEK ENDS

DSG  SEGMENT PARA 'DTS'
DIZI DW 1234H, 2345H, 0ABCDH, 0DEDEH, 0BABAH
;DIZI : 4321H, 5432H, 0DCBAH, 0EDEDH, 0ABABH
; AX:1234H
;AL:34H
;AH:12H
;XCHG AH, AL
;AL:12H
;AH:34H
;ROT AH, 4
;AH: 43H
;ROT AL, 4
;AL: 21H
;AX: 4321H

ELEMAN DW 5
DSG ENDS

CSG  SEGMENT PARA 'CODE'
        ASSUME CS:CSG, DS:DSG, SS:STEK

BASLA   PROC FAR

        PUSH DS
        XOR AX,AX
        PUSH AX

        MOV AX, DSG
        MOV DS, AX
	
	LEA DI, DIZI
	MOV CX, ELEMAN

	L1:
	MOV AX, [DI]
	XCHG AH, AL
	PUSH CX
	MOV CL, 4
	ROR AH, CL
	ROR AL, CL
	POP CX
	MOV [DI], AX
	ADD DI, 2
	LOOP L1
	


        RETF
BASLA   ENDP
CSG  ENDS
        END BASLA
