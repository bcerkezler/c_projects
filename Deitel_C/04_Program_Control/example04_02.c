// Counter-controlled iteration with the for statement.
#include <stdio.h>

int main() {

    for (int counter = 1; counter <= 5; ++counter) {
        printf("%d ", counter);
    }
    
    puts("");
}